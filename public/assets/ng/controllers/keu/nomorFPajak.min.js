var app = angular
          .module('appRoot', ['ngMessages','angularUtils.directives.dirPagination','ngRoute', 'currencyMask', 'ui.bootstrap'])
          .config(function($interpolateProvider){
              $interpolateProvider.startSymbol('[[');
              $interpolateProvider.endSymbol(']]');
          })
        .directive('loading', ['$http',function($http){
          return {
            restrict: 'A',
            link : function(scope, element, attrs){
              scope.isLoading = function(){
                return $http.pendingRequests.length > 0;
              };

              scope.$watch(scope.isLoading, function(value){
                if(value){
                  element.show(); 
                } else{
                  element.hide();
                }
              })
            }
          }
        }]);


app.controller('dataFormController', function($scope, $http, $uibModal, $log) {
  $scope.modalstate = {};
  $scope.parseInt = parseInt;
  $scope.libraryTemp = {};
  $scope.dataForm = {};
  $scope.totalItemsTemp = {};
  $scope.totalItems = 0;
  $scope.subUrl = '/keu/nomor_fpajak';
  $modalName ='idModalMst';
  $scope._token = {};
  $scope._pageNumber = 1;

    $http({
      'method'  : 'GET',
      'url'   : '/token' 
    }).success(function(response){
      //console.log(response);
      $scope._token     = response;
      
      //retrieve employees listing from API
      getResultsPage($scope._pageNumber);

    }).error(function(response){
        //console.log(response);
    });

  $scope.pageChanged = function(newPage) {
    getResultsPage(newPage);
  };

  $scope.searchDB = function(){
      if($.isEmptyObject($scope._token)) return false;
        if($scope.searchText.length >= 1){
          if($.isEmptyObject($scope.libraryTemp)){
                $scope.libraryTemp = $scope._data;
                $scope.totalItemsTemp = $scope.totalItems;
                $scope._data = {};
          }
          getResultsPage(1);
        }else{
            if(!$.isEmptyObject($scope.libraryTemp)){
                $scope._data = $scope.libraryTemp ;
                $scope.totalItems = $scope.totalItemsTemp;
                $scope.libraryTemp = {};
            }
        }
    } 


  function getResultsPage(pageNumber) {
      $scope.formTampil = false;      
      var _url = $scope._token['url']+ $scope.subUrl;
      if($.isEmptyObject($scope._token)) return false;
      
      $scope._pageNumber = pageNumber;
      
      if(! $.isEmptyObject($scope.libraryTemp)){
        $http({
          'method'  : 'GET',
          'url'   : _url+'?search='+$scope.searchText+'&page='+pageNumber 
        }).success(function(response){
          //console.log(response);
          $scope._data     = response.data;
          $scope.totalItems   = response.total;
          $scope.table = { TotalItems : response.total, CurrentItems : response.to };
        }).error(function(response){
          //console.log(response);
        });
      }else{
        $http({
          'method'  : 'GET',
          'url'   : _url+'?page='+pageNumber 
        }).success(function(response){
          //console.log(response);
          $scope._data        = response.data;
          $scope.totalItems   = response.total;
          $scope.table        = { TotalItems : response.total, CurrentItems : response.to };
        }).error(function(response){
           //console.log(response);
        });
      }
  }

    //show modal form
    $scope.toggle = function(modalstate, id){
      var _url = $scope._token['url']+ $scope.subUrl;
      if($.isEmptyObject($scope._token)) return false;

      $scope.modalstate = modalstate; 
      
      switch(modalstate){
        case 'add' :
          $scope.idState    = null;
          $scope.form_title = 'Tambah Data';
          $scope.dataForm   = {};
          $scope.formTampil = true;
        break;
        case 'edit' :
          $scope.form_title = 'Edit Data';
          $scope.idState = id;
          $http({
            'method'  : 'POST',
            'url'     : _url+'/pukisdev/edit',
            'params'  : { 'nomor_surat_pajak' : id}
          }).success(function(response){
              // showData(response);
              $scope.dataForm = response;
              $scope.dataForm['tanggal'] = new Date(response['tanggal']);

          }).error(function(response){
            console.log(response);
          });

          $scope.formTampil = true;
        break;
        case 'cancel':
          getResultsPage($scope._pageNumber);
        break; 
        default:
        break;
      }
    }

    $scope.save = function(modalstate, id){
      var _url = $scope._token['url']+ $scope.subUrl;
      if($.isEmptyObject($scope._token)) return false;

      $http({
        'method'  : (id) ? 'PUT' : 'POST',
        'url'     : _url + ((id) ? '/pukisdev' : ''),
        'data'    : $.param($scope.dataForm),
        'headers' : {'Content-Type': 'application/x-www-form-urlencoded'}
      }).success(function(response){
          new PNotify({
              title : 'Berhasil',
              text  : 'Edit Data Berhasil Disimpan',
              type  : 'success'
          });
   
        if(response.nomor_surat_pajak){          
          $scope.dataForm = response;
          $scope.dataForm['tanggal'] = new Date(response['tanggal']);
          $scope.modalstate = 'edit';
          $scope.idState = response.nomor_surat_pajak;
        }

      }).error(function(error){
          new PNotify({
              title : 'Error',
              text  : 'Data Gagal Disimpan',
              type  : 'error'
          });
        console.log(error);
      });
    }

  $scope.confirmDelete = function(_data, _index){
      var _url = $scope._token['url']+ $scope.subUrl;
      if($.isEmptyObject($scope._token)) return false;
      
      var isConfirmDelete = confirm('Anda yakin mau menghapus data ini?');
      if(isConfirmDelete){
        $http({
          'method'  : 'DELETE',
          'url'     : _url+'/pukisdev', 
          'data'    : $.param(_data),
          'headers' : {'Content-Type': 'application/x-www-form-urlencoded'}
        }).success(function(response){
          $scope._data.splice(_index,1);
            new PNotify({
              title : 'Berhasil',
              text  : 'Data Berhasil Dihapus',
              type  : 'success'
          });
        }).error(function(response){
          //console.log(response);
          new PNotify({
              title : 'Gagal',
              text  : 'Data Gagal Dihapus',
              type  : 'error'
          });
        });
      }
    }



/*


  $scope.lovSelected = function(idModal, _element){
    
    if($.isEmptyObject($scope.dataForm)){
      $scope.dataForm = {};
    }

    angular.forEach(_element, function (item,key) {
      // console.log(key + '||' +key.indexOf('id'));
      key = (key.indexOf('id') > -1) ? 'f_'+key.substr(3) : key;
      $scope.dataForm[key] = item;
    });
    
    $('#'+idModal).modal('hide');
  }

  $scope.showModal = function (idModal){
    $('#'+idModal).modal('show');
  }

  $scope.selectFile = function(_name){
    // console.log($scope.dataForm[_name].name);
    $scope.dataForm[_name.slice(1)] =  $scope.dataForm[_name].name;
  }

  $scope.getFiles = function(_name){
    var _url = $scope._token['url']+ $scope.subUrl;
    if($.isEmptyObject($scope._token)) return false;
    window.open(_url+'/files/'+_name+'/'+$scope.dataForm[_name], '_blank'); 
  }

  $scope.setApv = function(_answer, _index){
      var _url = $scope._token['url']+ $scope.subUrl;
      if($.isEmptyObject($scope._token)) return false;

      //console.log(_answer+'/'+_index);
      $http({
        'method'  : 'PUT',
        'url'     : _url+'/'+$scope.dataForm['id_spks']+'/apv', 
        'data'    : { 'jawaban' : _answer, 'komentar' : $scope.dataForm['apv'][_index]['komentar'], 'apv_nip' : $scope.dataForm['apv'][_index]['apv_nip'] },
        // 'headers' : {'Content-Type': 'application/x-www-form-urlencoded'}
      }).success(function(response){
        $scope.dataForm['apv'][_index]['apv_status']=_answer;
        // // console.log(response);
        // $scope._data     = response.data;
        // $scope.totalItems   = response.total;
        // $scope.table = { TotalItems : response.total, CurrentItems : response.to };
      }).error(function(response){
        //console.log(response);
      });

  }  

  $scope.lovNpb = function (_index, _tipe, _value) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      // console.log('mane?');
    $scope.params = { };//_f_customer : (_customer) ? _customer : $scope.dataForm['f_customer']  };
    var modalInstance = $uibModal.open({
      animation: true,
      templateUrl:  '/assets/ng/views/keu/lov/lovNpb.html', 
      controller:   'lovNpbController',
      size: 'lg',
      resolve: {
        parameter : function(){
          //console.log($scope.params);
          return $scope.params;
        }, 
      }
    });
    modalInstance.result.then(function (_element) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      if(_tipe === 'tambah'){// && $scope.dataForm['det'][0]['no'] !== ''){
        var _data           = new Object();
        _data.no_customer   = _element.no_pp;
        _data.keterangan    = _element.keterangan;
        _data.currency_id   = 'IDR';
        _data.nilai         = _element.total_bayar;
        $scope.dataForm['bpk'].push(_data);
      } else if(_index || _index === 0){
        console.log(_element);
        
        $scope.dataForm['bpk'][_index]= {} ;
        $scope.dataForm['bpk'][_index]['no_customer']     = _element.no_pp;
        $scope.dataForm['bpk'][_index]['keterangan']      = _element.keterangan;
        $scope.dataForm['bpk'][_index]['currency_id']     = _element.currency_id;
        $scope.dataForm['bpk'][_index]['nilai']           = _element.total_bayar;
      } 
    }, function () {
      $log.info('Modal dismissed at: ' + new Date());
    });
  }

  $scope.rmLovNpb = function (_data, _pp, _index) {
      var _url = $scope._token['url'];
      if($.isEmptyObject($scope._token)) return false;

      var isConfirmDelete = confirm('Anda yakin mau menghapus data ini?');
      if(isConfirmDelete){
        if(_pp){        
          $http({
            'method'  : 'DELETE',
            'url'     : _url+'/keu/pembayaran/det/bukti',
            'params'  : { 'no_pp' : _pp, 'data' : _data } 
          }).success(function(response){

          }).error(function(response){
            console.log(response);
            $.gritter.add({
                  title: 'Application',
                  text: 'Data Gagal Dihapus',
                  fade_out_speed: 5000
              });
          });
        }
        
        $scope.dataForm['bpk'].splice(_index,1);          
      }
  }

  $scope.lovBank = function () {
    if($.isEmptyObject($scope.dataForm)){
      $scope.dataForm = {};
    }

    $scope.params     = {}
    var modalInstance = $uibModal.open({
      animation   : true,
      templateUrl : '/assets/ng/views/keu/lov/lovBank.html', 
      controller  : 'lovBankController',
      size        : 'lg',
      resolve     : {
        parameter : function(){
          //console.log($scope.params);
          return $scope.params;
        }, 
      }
    });
    modalInstance.result.then(function (_element) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
        $scope.dataForm['no_bank']    = _element.no;
        $scope.dataForm['nama_bank']  = _element.nama_bank;
        $scope.dataForm['no_acc']     = _element.no_acc;
    }, function () {
      $log.info('Modal dismissed at: ' + new Date());
    });
  }

  $scope.lovSeri = function (_index, _tipe, _value) {
     if($.isEmptyObject($scope.dataForm)){
       $scope.dataForm = {};
    }
      // console.log('mane?');
    $scope.params = { };//_f_customer : (_customer) ? _customer : $scope.dataForm['f_customer']  };
    var modalInstance = $uibModal.open({
      animation: true,
      templateUrl:  '/assets/ng/views/keu/lov/lovSeri.html', 
      controller:   'lovSeriController',
      size: 'lg',
      resolve: {
        parameter : function(){
          //console.log($scope.params);
          return $scope.params;
        }, 
      }
    });
    modalInstance.result.then(function (_element) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      if(_tipe === 'tambah'){// && $scope.dataForm['det'][0]['no'] !== ''){
        console.log(_element);
        var _data          = new Object();
        _data.no_seri      = _element.no_giro;
        _data.no_bank      = _element.fa_bank_mst.no;
        _data.nama_bank    = _element.fa_bank_mst.nama_bank;
        _data.jatuh_tempo  = new Date(_element.jatuh_tempo);
        _data.keterangan   = _element.keterangan;
        _data.jumlah       = _element.jumlah;
        $scope.dataForm['det'].push(_data);
      } else if(_index || _index === 0){
        //console.log(_element.no_pp+'/'+_index);
        
        $scope.dataForm['det'][_index]                = {} ;
        $scope.dataForm['det'][_index]['no_seri']     = _element.no_giro;
        $scope.dataForm['det'][_index]['no_bank']     = _element.fa_bank_mst.no;
        $scope.dataForm['det'][_index]['nama_bank']   = _element.fa_bank_mst.nama_bank;
        $scope.dataForm['det'][_index]['jatuh_tempo'] = new Date(_element.jatuh_tempo);
        $scope.dataForm['det'][_index]['keterangan']  = _element.keterangan;
        $scope.dataForm['det'][_index]['jumlah']      = _element.jumlah;
      } 
    }, function () {
      $log.info('Modal dismissed at: ' + new Date());
    });
  }

  $scope.rmLovSeri = function (_data, _pp, _index) {
      var _url = $scope._token['url'];
      if($.isEmptyObject($scope._token)) return false;

      var isConfirmDelete = confirm('Anda yakin mau menghapus data ini?');
      if(isConfirmDelete){
        if(_pp){        
          $http({
            'method'  : 'DELETE',
            'url'     : _url+'/keu/pembayaran/det/bukti',
            'params'  : { 'no_pp' : _pp, 'data' : _data } 
          }).success(function(response){

          }).error(function(response){
            console.log(response);
            $.gritter.add({
                  title: 'Application',
                  text: 'Data Gagal Dihapus',
                  fade_out_speed: 5000
              });
          });
        }
        
        $scope.dataForm['det'].splice(_index,1);          
      }
  }

  $scope.lovAccCoa = function (_index, _tipe, _value) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      // console.log('mane?');
    $scope.params = { };//_f_customer : (_customer) ? _customer : $scope.dataForm['f_customer']  };
    var modalInstance = $uibModal.open({
      animation: true,
      templateUrl:  '/assets/ng/views/keu/lov/lovAccCoa.html', 
      controller:   'lovAccCoaController',
      size: 'lg',
      resolve: {
        parameter : function(){
          //console.log($scope.params);
          return $scope.params;
        }, 
      }
    });
    modalInstance.result.then(function (_element) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      if(_tipe === 'tambah'){
        var _data           = new Object();
        _data.coa_id        = _element.coa_id;
        _data.coa_name      = _element.coa_name;
        _data.currency_id   = 'IDR';
        $scope.dataForm['jurnal_det'].push(_data);
      } else if(_index || _index === 0){

        $scope.dataForm['jurnal_det'][_index]['coa_id']     = _element.coa_id;
        $scope.dataForm['jurnal_det'][_index]['coa_name']   = _element.coa_name;
      } 
    }, function () {
      $log.info('Modal dismissed at: ' + new Date());
    });
  }

  $scope.rmLovAccCoa = function (_data, _pp, _index) {
      var _url = $scope._token['url'];
      if($.isEmptyObject($scope._token)) return false;

      var isConfirmDelete = confirm('Anda yakin mau menghapus data ini?');
      if(isConfirmDelete){
        if(_pp){        
          $http({
            'method'  : 'DELETE',
            'url'     : _url+'/keu/pembayaran/det/bukti',
            'params'  : { 'no_pp' : _pp, 'data' : _data } 
          }).success(function(response){

          }).error(function(response){
            console.log(response);
            $.gritter.add({
                  title: 'Application',
                  text: 'Data Gagal Dihapus',
                  fade_out_speed: 5000
              });
          });
        }
        
        $scope.dataForm['jurnal_det'].splice(_index,1);          
      }
  }

  $scope.lovCostCenter = function (_index, _tipe, _value) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      // console.log('mane?');
    $scope.params = {};//_f_customer : (_customer) ? _customer : $scope.dataForm['f_customer']  };
    var modalInstance = $uibModal.open({
      animation: true,
      templateUrl:  '/assets/ng/views/keu/lov/lovCostCenter.html', 
      controller:   'lovCostCenterController',
      size: 'lg',
      resolve: {
        parameter : function(){
          //console.log($scope.params);
          return $scope.params;
        }, 
      }
    });
    modalInstance.result.then(function (_element) {
      if($.isEmptyObject($scope.dataForm)){
        $scope.dataForm = {};
      }
      if(_tipe === 'tambah'){// && $scope.dataForm['det'][0]['no'] !== ''){
        var _data               = new Object();
        _data.costcenter        = _element.cost_id;
        _data.cost_keterangan   = _element.cost_keterangan;
        $scope.dataForm['jurnal_det'].push(_data);
      } else if(_index || _index === 0){
        $scope.dataForm['jurnal_det'][_index]['costcenter']       = _element.cost_id;
        $scope.dataForm['jurnal_det'][_index]['cost_keterangan']  = _element.cost_keterangan;
      } 
    }, function () {
      $log.info('Modal dismissed at: ' + new Date());
    });
  }

  $scope.save = function(modalstate, id){
    var _url = $scope._token['url']+ $scope.subUrl;
    if($.isEmptyObject($scope._token)) return false;
    $scope.dataForm['keterangan'] = $scope.dataForm['keterangan'].toString();

    $http({
      'method'  : ($scope.dataForm['no_bukti']) ? 'PUT' : 'POST',
      'url'     : _url + (($scope.dataForm['no_bukti']) ? '/'+$scope.dataForm['no_bukti'] : ''),
      'data'    : $.param($scope.dataForm),
      'headers' : {'Content-Type': 'application/x-www-form-urlencoded'}
    }).success(function(response){
      if(modalstate === 'edit') {
          new PNotify({
              title : 'Berhasil',
              text  : 'Edit Data Berhasil',
              type  : 'success'
          });
      }
      else {
          new PNotify({
              title : 'Berhasil',
              text  : 'Input Data Berhasil',
              type  : 'success'
          });
      }
      showData(response);
      $scope.modalstate = 'edit'; 

      $.gritter.add({
            title: 'Application',
            text: 'Data Berhasil Disimpan',
            fade_out_speed: 5000
        });
    }).error(function(error){
      //console.log(error);
    });
  }

  $scope.totalMst = function(){
    var _jumlah  = 0;
      angular.forEach($scope.dataForm['det'], function (item,key) {
          _jumlah  += parseInt(item.jumlah);
       });
      $scope.dataForm['jumlah']  = _jumlah;
  }

  $scope.totalGL = function(){
    var _debet  = 0;
    var _kredit = 0;
      angular.forEach($scope.dataForm['jurnal_det'], function (item,key) {
          _debet  += parseInt(item.debet);
          _kredit += parseInt(item.kredit);
      });
      $scope.dataForm['jml_debet']  = _debet;
      $scope.dataForm['jml_kredit'] = _kredit;
  }


  function showData(_dataTampil){
    //totalMst();
    console.log(_dataTampil);
    $scope.dataForm               = {};
    $scope.dataForm['det']        = [];
    $scope.dataForm['jurnal_det'] = [];

    $scope.dataForm                 = _dataTampil;
    $scope.dataForm['tgl_cetak']    = new Date(_dataTampil['tgl_cetak']);
    $scope.dataForm['from_periode'] = new Date(_dataTampil['from_periode']);
    $scope.dataForm['to_periode']   = new Date(_dataTampil['to_periode']);
    $scope.dataForm['no_bank']      = _dataTampil['fa_bank_mst']['no']
    $scope.dataForm['nama_bank']    = _dataTampil['fa_bank_mst']['nama_bank']
    $scope.dataForm['no_acc']       = _dataTampil['fa_bank_mst']['no_acc'];
    


    for(det in _dataTampil.fa_setoran_bank_det){
      _dataTampil.fa_setoran_bank_det[det]['jatuh_tempo']   =  new Date(_dataTampil.fa_setoran_bank_det[det]['jatuh_tempo']);
      _dataTampil.fa_setoran_bank_det[det]['keterangan']    = _dataTampil.fa_setoran_bank_det[det]['fa_penerimaan_det']['keterangan'];
      _dataTampil.fa_setoran_bank_det[det]['no_seri']       = _dataTampil.fa_setoran_bank_det[det]['fa_penerimaan_det']['no_giro'];
      _dataTampil.fa_setoran_bank_det[det]['no_bank']       = _dataTampil.fa_setoran_bank_det[det]['fa_penerimaan_det']['no_bank'];
      _dataTampil.fa_setoran_bank_det[det]['nama_bank']     = _dataTampil.fa_setoran_bank_det[det]['fa_penerimaan_det']['fa_bank_mst']['nama_bank'];
      
      delete _dataTampil.fa_setoran_bank_det[det]['fa_penerimaan_det'];
    }
    $scope.dataForm['det']  = _dataTampil.fa_setoran_bank_det;

    for(jurnal_det in _dataTampil.fa_setoran_bank_jurnal_det){
      _dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['coa_name']        =  (_dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['acc_coas_mst']) ? _dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['acc_coas_mst']['coa_name'] : null;
      _dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['cost_keterangan'] =  (_dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['acc_cost_center']) ? _dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['acc_cost_center']['cost_keterangan'] : null;
      
      delete _dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['acc_coas_mst'];
      delete _dataTampil.fa_setoran_bank_jurnal_det[jurnal_det]['acc_cost_center'];
    }  
    $scope.dataForm['jurnal_det']  = _dataTampil.fa_setoran_bank_jurnal_det;
    
    console.log($scope.dataForm['jurnal_det']);
    //delete $scope.dataForm['fa_bank_mst'];
  }
*/
});

app.directive('formData', function (){
    return{
        restirct    : 'A',
        templateUrl : '/assets/ng/views/keu/form/formNomorFPajak.html',
    };
});

app.directive('lovCustomer', function (){
    return{
        restirct    : 'A',
        templateUrl : '/ext/ng-html/PMS/lov/lovCustomer.html',
    };
});


function apiModifyTable(originalData,id,response){
    angular.forEach(originalData, function (item,key) {
      // console.log(item.id_customer +' \\ '+key+' / '+id);
        if(item.no == id){
            originalData[key] = response;
        }
    });
    return originalData;
}