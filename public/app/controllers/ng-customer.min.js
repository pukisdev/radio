app.controller('customerController', function($scope, $http, API_URL) {
	$scope.libraryTemp = {};
  $scope.totalItemsTemp = {};
  $scope.totalItems = 0;
  $scope.subUrl = 'pms/customer';
  $modalName ='idModalMst';
  $scope._token = {};


    $http({
      'method'  : 'GET',
      'url'   : '/token' 
    }).success(function(response){
      // console.log(response);
      $scope._token     = response;

      //retrieve employees listing from API
      getResultsPage(1);

    }).error(function(response){
        console.log(response);
    });



    //show modal form
    $scope.toggle = function(modalstate, id){
      if($.isEmptyObject($scope._token)) return false;

      $scope.modalstate = modalstate; 

      switch(modalstate){
        case 'add' :
          $scope.form_title = 'Tambah Data';
          $scope.dataForm = null;
        break;
        case 'edit' :
          $scope.form_title = 'Edit Data';
          $scope.idState = id;
          $http.get(API_URL + $scope.subUrl+"/" + id)
            .success(function(response){
              // console.log(response);
              $scope.dataForm = response;
            });
        break;
        default:
        break;
      }

      $('#'+$modalName).modal('show');
    }

    $scope.save = function(modalstate, id){
      if($.isEmptyObject($scope._token)) return false;

      var _url = API_URL + $scope.subUrl;
      var _method = (modalstate === 'edit') ? 'PUT' : 'POST';

      if(modalstate ==='edit') _url += '/'+id;

      // console.log(id);
      $http({
        'method'  : _method,
        'url'     : _url,
        'data'    : $.param($scope.dataForm),
        'headers' : {'Content-Type': 'application/x-www-form-urlencoded'}
      }).success(function(response){
        // console.log(response);
        if(modalstate === 'edit') {
          $scope._data = apiModifyTable($scope._data, response.id_customer, response);
        }
        else {
          // console.log(response);
          $scope._data.push(response);
        }
          
        $('#'+$modalName).modal('hide');
        $.gritter.add({
              title: 'Application',
              text: 'Data Berhasil Disimpan',
              fade_out_speed: 5000
          });
      }).error(function(error){
        console.log(error);
      });

    }

    $scope.confirmDelete = function(_data, _index){
      if($.isEmptyObject($scope._token)) return false;
      
      var isConfirmDelete = confirm('Anda yakin mau menghapus data ini?');
      if(isConfirmDelete){
        $http({
          'method'  : 'DELETE',
          'url'     : API_URL+$scope.subUrl+'/'+_data.id_customer 
        }).success(function(response){
          $scope._data.splice(_index,1);
          $.gritter.add({
                title: 'Application',
                text: 'Data Berhasil Dihapus',
                fade_out_speed: 5000
            });
        }).error(function(response){
          console.log(response);
          $.gritter.add({
                title: 'Application',
                text: 'Data Gagal Dihapus',
                fade_out_speed: 5000
            });
        });
      }
    }
  
    $scope.searchDB = function(){
      if($.isEmptyObject($scope._token)) return false;
        if($scope.searchText.length >= 1){
          // console.log($scope.libraryTemp);
          if($.isEmptyObject($scope.libraryTemp)){
                $scope.libraryTemp = $scope._data;
                $scope.totalItemsTemp = $scope.totalItems;
                $scope._data = {};
          }
          getResultsPage(1);
        }else{
          // console.log('masuk');
            if(!$.isEmptyObject($scope.libraryTemp)){
                $scope._data = $scope.libraryTemp ;
                $scope.totalItems = $scope.totalItemsTemp;
                $scope.libraryTemp = {};
            }
        }
    }

    $scope.pageChanged = function(newPage) {
      getResultsPage(newPage);
    };
/*

*/
  function getResultsPage(pageNumber) {
          // console.log($scope._token['token']);
      if(! $.isEmptyObject($scope.libraryTemp)){
        $http({
          'method'  : 'GET',
          // 'url'   : API_URL+$scope.subUrl+'?search='+$scope.searchText+'&page='+pageNumber 
          'url'   : 'http://lumen.api/mst/pms/customer?_token='+$scope._token['token']+'&_e='+$scope._token['e']+'&search='+$scope.searchText+'&page='+pageNumber 
          // 'url'   : 'http://lumen.api/mst/pms/customer?_token='+{{ Auth::user()->remember_token }}+'&_e='+{{ base64_encode(Auth::user()->email)}}+'&search='+$scope.searchText+'&page='+pageNumber 
        }).success(function(response){
          // console.log(response);
          $scope._data     = response.data;
          $scope.totalItems   = response.total;
          $scope.table = { TotalItems : response.total, CurrentItems : response.to };
        }).error(function(response){
            console.log(response);
        });
      }else{
        $http({
          'method'  : 'GET',
          // 'method'  : 'JSONP',
          // 'url'   : API_URL+$scope.subUrl+'?page='+pageNumber 
          'url'   : 'http://lumen.api/mst/pms/customer?_token='+$scope._token['token']+'&_e='+$scope._token['e']+'&page='+pageNumber 
        }).success(function(response){
          // console.log(response);
          $scope._data     = response.data;
          $scope.totalItems   = response.total;
          $scope.table = { TotalItems : response.total, CurrentItems : response.to };
        }).error(function(response){
          console.log(response);
        });
      }
  }

});

app.directive('modalCustomer', function (){
    return{
        restirct    : 'A',
        templateUrl : '/ext/ng-html/modalCustomer.html',
    };
});

function apiModifyTable(originalData,id,response){
    angular.forEach(originalData, function (item,key) {
      // console.log(item.id_customer +' \\ '+key+' / '+id);
        if(item.id_customer == id){
            originalData[key] = response;
        }
    });
    return originalData;
}